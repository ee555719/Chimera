<Window x:Class="Chimera.Host.Pages.Security.PermissionPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="权限请求" Height="280" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="⚠ 插件权限请求" FontSize="18" FontWeight="Bold" Foreground="#D83B01"/>
            <TextBlock x:Name="PluginInfo" Text="" FontSize="12" Foreground="Gray" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Permission Details -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                CornerRadius="4" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="该插件请求以下权限:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBlock x:Name="PermissionName" Text="" FontSize="16" FontWeight="Bold" Foreground="#0078D4"/>
                <TextBlock x:Name="PermissionDescription" Text="" FontSize="12" Foreground="#555" 
                           TextWrapping="Wrap" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Remember Choice -->
        <CheckBox Grid.Row="2" x:Name="RememberChoiceCheckBox" Content="记住此选择（不再询问）" 
                  Margin="0,0,0,16" IsChecked="False"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="DenyButton" Content="拒绝" Width="80" Height="32" 
                    Margin="0,0,8,0" Click="DenyButton_Click"
                    Background="#E8E8E8" Foreground="#333" BorderThickness="0"/>
            <Button x:Name="AllowButton" Content="允许" Width="80" Height="32"
                    Click="AllowButton_Click"
                    Background="#0078D4" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
