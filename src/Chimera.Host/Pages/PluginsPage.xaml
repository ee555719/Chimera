<UserControl x:Class="Chimera.Host.Pages.PluginsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="已安装插件" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="安装本地插件" Click="InstallPlugin_Click"
                    Padding="16,6" Background="#0078D4" Foreground="White" BorderThickness="0"/>
        </Grid>

        <!-- Plugin List -->
        <ListView x:Name="PluginListView" Grid.Row="1" BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="版本" DisplayMemberBinding="{Binding Version}" Width="80"/>
                    <GridViewColumn Header="作者" DisplayMemberBinding="{Binding Author}" Width="120"/>
                    <GridViewColumn Header="状态" DisplayMemberBinding="{Binding Status}" Width="80"/>
                    <GridViewColumn Header="操作" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="卸载" Click="UninstallPlugin_Click" Tag="{Binding Id}" 
                                            Margin="0,0,8,0" Padding="8,2"/>
                                    <CheckBox Content="启用" IsChecked="{Binding IsEnabled}" 
                                              Checked="TogglePlugin_Checked" Unchecked="TogglePlugin_Unchecked"
                                              Tag="{Binding Id}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="FooterInfo" Text="" Foreground="Gray" VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="RestartButton" Content="重启" Click="Restart_Click"
                        Padding="16,6" Background="#FF6B35" Foreground="White" BorderThickness="0"
                        Margin="0,0,8,0" ToolTip="重启应用以生效新安装的插件"/>
                <Button x:Name="PackButton" Content="打包" Click="Pack_Click"
                        Padding="16,6" Background="#28A745" Foreground="White" BorderThickness="0"
                        ToolTip="将当前配置打包成独立exe"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
