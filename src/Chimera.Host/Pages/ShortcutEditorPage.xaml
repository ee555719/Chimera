<UserControl x:Class="Chimera.Host.Pages.ShortcutEditorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="快捷键编辑器" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="添加快捷键" Click="AddShortcut_Click" Margin="16,0,0,0"
                    Padding="12,6" Background="#0078D4" Foreground="White" BorderThickness="0"/>
            <Button Content="导出配置" Click="ExportConfig_Click" Margin="8,0,0,0" Padding="12,6"/>
            <Button Content="导入配置" Click="ImportConfig_Click" Padding="12,6"/>
        </StackPanel>

        <!-- Shortcuts List -->
        <ListView x:Name="ShortcutsList" Grid.Row="1" BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="快捷键" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0}+{1}">
                                            <Binding Path="ModifiersText"/>
                                            <Binding Path="Key"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="插件" DisplayMemberBinding="{Binding PluginId}" Width="120"/>
                    <GridViewColumn Header="状态" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsEnabled}" 
                                          Checked="ShortcutEnabled_Changed"
                                          Unchecked="ShortcutEnabled_Changed"
                                          Tag="{Binding Id}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="操作" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="编辑" Click="EditShortcut_Click" Tag="{Binding Id}"
                                            Padding="8,2" Margin="0,0,4,0"/>
                                    <Button Content="删除" Click="DeleteShortcut_Click" Tag="{Binding Id}"
                                            Padding="8,2" Foreground="Red"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Footer -->
        <TextBlock Grid.Row="2" x:Name="FooterInfo" Text="" Foreground="Gray" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
