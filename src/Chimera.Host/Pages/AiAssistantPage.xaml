<UserControl x:Class="Chimera.Host.Pages.AiAssistantPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="600">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="AI 助手" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="清除历史" Click="ClearHistory_Click" Margin="16,0,0,0" Padding="8,4"/>
        </StackPanel>

        <!-- Chat Messages -->
        <ScrollViewer x:Name="ChatScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ChatMessagesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="12" 
                                Background="{Binding IsUserMessage, Converter={StaticResource MessageBackgroundConverter}}"
                                CornerRadius="8" MaxWidth="80">
                            <StackPanel>
                                <TextBlock Text="{Binding RoleDisplay}" FontWeight="SemiBold" 
                                           FontSize="11" Foreground="Gray" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Suggestions -->
        <ItemsControl x:Name="SuggestionsList" Grid.Row="2" Margin="0,8,0,8">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Click="Suggestion_Click" 
                            Padding="8,4" Margin="4,0" Style="{StaticResource SuggestionButtonStyle}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Input -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="MessageInput" Grid.Column="0" 
                     KeyDown="MessageInput_KeyDown"
                     Margin="0,0,8,0" Padding="8"/>
            
            <Button x:Name="SendButton" Grid.Column="1" Content="发送" 
                    Click="SendButton_Click" Padding="12,8"
                    Background="#0078D4" Foreground="White" BorderThickness="0"/>
        </Grid>
    </Grid>
</UserControl>
