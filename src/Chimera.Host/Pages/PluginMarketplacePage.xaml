<UserControl x:Class="Chimera.Host.Pages.PluginMarketplacePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="700">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="插件市场" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- Search and Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" Grid.Column="0" 
                     Margin="0,0,8,0" KeyDown="SearchBox_KeyDown"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="搜索" Click="Search_Click" Padding="12,6" Margin="0,0,8,0"/>
                <Button Content="检查更新" Click="CheckUpdates_Click" Padding="12,6"/>
            </StackPanel>
        </Grid>

        <!-- Plugins List -->
        <ListView x:Name="PluginsList" Grid.Row="2" BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Id}" FontSize="10" Foreground="Gray"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="版本" DisplayMemberBinding="{Binding Version}" Width="80"/>
                    <GridViewColumn Header="作者" DisplayMemberBinding="{Binding Author}" Width="100"/>
                    <GridViewColumn Header="描述" Width="*">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Description}" TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="标签" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding Tags}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#E8F0FE" CornerRadius="4" 
                                                    Padding="4,2" Margin="2">
                                                <TextBlock Text="{Binding}" FontSize="10"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="操作" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="安装" Click="InstallPlugin_Click" 
                                        Tag="{Binding}" Padding="8,4"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="StatusText" Text="" Foreground="Gray" VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="上一页" Click="PreviousPage_Click" Padding="8,4" Margin="0,0,4,0"/>
                <TextBlock x:Name="PageInfo" Text="" VerticalAlignment="Center" Margin="8,0"/>
                <Button Content="下一页" Click="NextPage_Click" Padding="8,4"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
